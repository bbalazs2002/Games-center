# Node / build output
node_modules/
dist/
build/
.env
.env.local

# temp/ is a working/scratch area (see docs/Projekt-conception.md) — not versioned
temp/**
!temp/.gitkeep

# assets contains large files that are not versioned yet
assets/**

# opt-in full game-event logs (docs/hotel-0d-ai-specifikacio.md §4.8) — local analysis artifacts, not versioned
logs/**

# OS
.DS_Store
Thumbs.db
