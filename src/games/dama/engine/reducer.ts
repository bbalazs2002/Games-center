import type { DamaAction } from './actions';
import type { DamaState } from './state';

/**
 * TODO(Fázis 1): implement the move/capture/promotion/win rules described in
 * docs/fazis-0a-dama-specifikacio.md §3.4. Not part of the Fázis 0a skeleton.
 */
export function reducer(_state: DamaState, _action: DamaAction): DamaState {
  throw new Error('Dáma reducer not implemented yet — see docs/fazis-0a-dama-specifikacio.md §3.4');
}
